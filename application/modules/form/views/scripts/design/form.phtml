<?php /** @var Dlayer_View_Codehinting */ $this ?>
<script>
$(document).ready(function() {
	dlayer.tools.form.fn.toggleTestWidths();
});
</script>

<style>
#design > div.builder-form * {
	font-family: <?php echo $this->base_font_family; ?>;
}

<?php foreach ($this->field_styles as $field_id => $styles) {
	if(count($styles) > 0) {
		echo '#design .row_' . $field_id . " {\r\n";
		foreach($styles as $style) {
			echo $style . "\r\n";
		}
	echo "}\r\n";
	}
} 
?>
</style>

<div class="row">
	<div class="col-md-8 col-sm-8 col-xs-12">
		<p class="dev-note bg-info"><strong>Note:</strong> The form will not 
			be this size when imported into the Content manager/Widget 
			designer, it will automatically size itself to fill the container 
			it is placed into.</p>
	</div>
	
	<div class="col-md-4 col-sm-4 col-xs-12">
		<p><strong>Test form widths</strong></p>
		<div class="btn-group set-form-display-size">
			<a title="" href="#" id="size-12" class="btn btn-primary btn-sm">100%</a>
			<a title="" href="#" id="size-9" class="btn btn-default btn-sm">75%</a>
			<a title="" href="#" id="size-6" class="btn btn-default btn-sm">50%</a>
			<a title="" href="#" id="size-3" class="btn btn-default btn-sm">25%</a>
		</div>	
	</div>
</div>

<div class="builder-form">
	<h3>
	<?php 
		echo $this->escape($this->titles['title']);
		if(strlen($this->titles['sub_title']) > 0) {
			echo ' <small>' . $this->escape($this->titles['sub_title']) . 
				'</small>';
		}
	
	?>
	</h3>
	<?php echo $this->form; ?>
</div>
