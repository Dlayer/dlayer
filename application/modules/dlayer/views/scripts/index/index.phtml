<?php /** @var $this Dlayer_View_Codehinting */?>

<div class="row">
	<div class="col-md-6 col-sm-6 col-xs-12">
		<?php echo $this->form; ?>

        <?php if ($this->test_credentials !== false) { ?>
		
		<h3>Test credentials.</h3>
		
		<p>Please use one of the free combinations below to sign in to the demo.</p>
			
		<ul>
			<?php foreach($this->test_credentials as $identity) { ?>
				<li>
				<strong>Username:</strong> <?php echo $this->escape($identity['identity']); ?><br />
				<strong>Password:</strong> password-<?php echo $this->escape($identity['id']); ?> 
				<?php if($identity['logged_in'] == 1) { ?> 
				<span class="label label-danger">Account signed in</span>
				<?php } else { ?>
				<span class="label label-success">Account free</span>
				<?php } ?>
				</li>
			<?php } ?>
		</ul>

        <?php } else { ?>
        <h3>No account?</h3>
        <p>Please ask your site administrator to create an account if you don't have one.</p>
        <?php } ?>
	</div>

    <div class="col-md-6 col-sm-6 col-xs-12">
        <h3><span class="label label-success">v1.06</span> Responsive columns <small class="text-muted">(In progress)</small></h3>

        <ul>
            <li>Removed additional terminators in SQL data file [Bugfix]</li>
            <li>Added a settings sub tool to columns, allows the column width and offset to defined. [Feature]</li>
            <li>Added a glyph style field to tool tabs table, added to allow glyph icons to be rotated.</li>
            <li>Moved column type from the columns structure table, now a separate table.</li>
            <li>Added a responsive sub tool to columns, allows the column with to be defined for the 'xs', 'sm' and
                'lg' layouts, Dlayer defaults to 'md'. [Feature]</li>
            <li>Added fixed control bar to the bottom of the designer, houses the cancel button and navigation
                controls for Content Manager. [UX]</li>
            <li>The tool buttons are now in the new control bar. [UX]</li>
        </ul>

        <h3><span class="label label-default">v1.05</span> Database setup <small class="text-muted">(17th January 2017)</small></h3>

        <h3><span class="label label-default">v1.04</span> Bugs... <small class="text-muted">(9th January 2017)</small></h3>

        <h3><span class="label label-default">v1.03</span> UX improvements <small class="text-muted">(7th January 2017)</small></h3>

        <p>The full <a href="http://www.dlayer.com/releases.html">release history</a> is available at Dlayer.com and
            on <a href="https://github.com/Dlayer/dlayer/releases">GitHub</a>.</p>
    </div>
</div>
