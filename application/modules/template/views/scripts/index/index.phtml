<?php /** @var $this Dlayer_View_Codehinting */ ?>

<div id="content">
    <h1>Template designer</h1>
    
    <h2>Web site: <?php echo $this->escape($this->site['name']); ?></h2>
    
    <p>Using the template designer you create the templates for your content 
    pages, these define the basic structure of a page, as in where the menu, 
    header and footer are placed and any items that you want to appear on 
    multiple pages, for example a small contact form or the main menu.</p>
    
    <p>Templates are typically very simple, they are just for boxing out the 
    design, you add the flourish to your pages using widgets and forms and then 
    once a page has been created the content manager.</p>
    
    <p>There is no artificial limit to the number of templates you can create 
    for a web site. Templates can have widgets and forms attached to them, you 
    create the forms and widgets in the form builder and widget designer.</p>
    
    <p>Select a template from the list below, once a template has been 
    activated you can continue working on the design, edit it or copy it to 
    create a new template.</p>
    
    <h2>Templates</h2>
    
    <ul>
    	<?php foreach($this->templates as $template) { ?>    	
    		<?php if($template['id'] == $this->template_id) { ?>
    			<li><?php echo $this->escape($template['name']); ?>: 
    			<a href="/template/design/">Design</a> | 
    			<a href="/template/index/edit-template">Edit</a> | 
    			<a href="#">Copy</a> | Active</li>
    		<?php } else { ?>
    			<li><a href="/template/index/activate/template-id/<?php echo $this->escape($template['id']); ?>"><?php echo $this->escape($template['name']); ?></a></li>
    		<?php } ?>
    	<?php } ?>
        <li><a href="/template/index/new-template">New template</a></li>
    </ul>
</div>