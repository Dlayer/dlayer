<?php /** @var $this Dlayer_View_Codehinting */ ?>

<div class="row">
	<div class="col-md-12 col-xs-12">
		<h1>Template designer <small>Web site template management</small></h1>
	</div>

	<div class="col-md-12 col-xs-12">
		<h2>Web site 
		<small><?php echo $this->escape($this->site['name']); ?></small></h2>
	</div>

	<div class="col-md-12 col-xs-12">
		<p>A template defines the basic structure of a content page, all 
			content pages will be based on and build upon a template.</p>
			
		<p>Templates are typically simple although that is not a requirement, 
			think of them as merely a way of blocking out content areas, 
			the obvious examples being where the footer, header and main 
			content areas will be.</p>
			
		<p>There are no artificial limits to the number of templates you can 
			create for a web site, the number of pages based upon a template 
			or how many items can be added to them. Forms and widgets can be 
			added to templates but typically most of the content and flourish 
			that makes up a page will be added via the Content manager.</p>
			
		<p>When a new site is created the Template designer will be stocked 
			with a number of predefined templates which you are free to edit, 
			copy or delete.</p>
			
		<p>Although widgets and forms can be added to templates they cannot 
			be modified in the Template designer, all changes will need to be 
			made in either the Widget designer or Form builder, this is 
			easily reached via the item and after editing you can return to 
			the Template designer, it will be in the same state as when you 
			left it.</p>
			
		<p>Select a template from the list below, once a template has been 
			selected you can start or continue working on the design, edit 
			the base details or copy it to create a brand new template.</p>
		
		<h2>Templates <small>Templates for this web site</small></h2>
		
		<?php foreach($this->templates as $template) { ?>    	
			<?php if($template['id'] == $this->template_id) { ?>
				<p>
					<button type="button" class="btn btn-default btn-xs">
					<?php echo $this->escape($template['name']); ?></button>  
					<a href="/template/design/" class="btn btn-default btn-xs">Design</a>  
					<a href="/template/index/edit-template" class="btn btn-default btn-xs">Edit</a>  					<a href="#" class="btn btn-default btn-xs disabled">Copy</a>  
					<span class="label label-success">Selected</span>
				</p>
				<?php } else { ?>
				<p>
					<a href="/template/index/activate/template-id/<?php echo $this->escape($template['id']); ?>" 
					class="btn btn-default btn-xs">
					<?php echo $this->escape($template['name']); ?></a>
				</p>
				<?php } ?>
			<?php } ?>
		<p>
			<a href="/template/index/new-template" 
			class="btn btn-primary">New template</a>
		</p>
	</div>
</div>