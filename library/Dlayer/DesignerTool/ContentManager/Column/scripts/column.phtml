<?php /** @var $this Dlayer_View_Codehinting */?>

<div class="col-md-12 col-sm-12">
	<h3>Column selected</h3>

	<?php if ($this->column_contains_content === false) { ?>
        <h4>
			<span class="glyphicon glyphicon-play" aria-hidden="true"></span> Add a row
		</h4>
		<p>To add a new row please select the 'add row' tool.</p>

        <?php if ($this->column_contains_rows === true) { ?>
		<h4>
			<span class="glyphicon glyphicon-play" aria-hidden="true"></span> Select a row
		</h4>
		<p>If a row is selectable it will highlight when you hover your cursor over it, to select the row, click.</p>
        <?php } ?>

	<?php } ?>

	<?php if ($this->column_contains_content === false && $this->column_contains_rows === false) { ?>

	<h4>Or...</h4>

	<?php } ?>

	<?php if ($this->column_contains_rows === false) { ?>
			
		<h4>
			<span class="glyphicon glyphicon-play" aria-hidden="true"></span> Add a content item
		</h4>

        <p>To add a new content item select the tool that corresponds to the content item you want to create.</p>

        <div class="row">
            <div class="col-md-12 col-sm-12">
                <div class="btn-group">
                <div class="btn-group">
                    <button type="button" class="btn btn-default dropdown-toggle" data-toggle="dropdown" aria-haspopup="true" aria-expanded="false">
                        Create tools <span class="caret"></span>
                    </button>
                    <ul class="dropdown-menu">
                        <?php foreach($this->layout()->control_bar_tools['create'] as $button) { ?>
                            <li><a href="<?php echo $this->escape($button['uri']); ?>"><?php echo $this->escape($button['name']); ?></a></li>
                        <?php } ?>
                    </ul>
                </div>

                <div class="btn-group">
                    <button type="button" class="btn btn-default dropdown-toggle" data-toggle="dropdown" aria-haspopup="true" aria-expanded="false">
                        Import tools <span class="caret"></span>
                    </button>
                    <ul class="dropdown-menu">
                        <?php foreach($this->layout()->control_bar_tools['import'] as $button) { ?>
                            <li><a href="<?php echo $this->escape($button['uri']); ?>"><?php echo $this->escape($button['name']); ?></a></li>
                        <?php } ?>
                    </ul>
                </div>
                </div>
            </div>
        </div>

        <?php if ($this->column_contains_content === true) { ?>
		<h4>
			<span class="glyphicon glyphicon-play" aria-hidden="true"></span> Edit an existing content item
		</h4>

		<p>If a content item is selectable it will highlight when you hover your cursor over it, to select the content
		item, click, alternatively, you select the content item using the button below:</p>

        <div class="row">
            <div class="col-md-12 col-sm-12">
                <div class="btn-group">
                    <div class="btn-group">
                        <button type="button" class="btn btn-default dropdown-toggle" data-toggle="dropdown" aria-haspopup="true" aria-expanded="false">
                            Content items <span class="caret"></span>
                        </button>
                        <ul class="dropdown-menu">
                            <li><a href="">Item 1</a></li>
                            <li><a href="">Item 2</a></li>
                        </ul>
                    </div>
                </div>
            </div>
        </div>
        <?php } ?>

		<h4>
			<span class="glyphicon glyphicon-play" aria-hidden="true"></span> Edit the column
		</h4>

		<p>To edit the properties for the column select a tab above.</p>

	<?php } ?>
</div>
